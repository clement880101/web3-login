# Automatically requests review from the repository owner on every pull request.
# This is a notification, not a gate: main does not require an approving review,
# so either maintainer can merge once CI passes.
* @clement880101
